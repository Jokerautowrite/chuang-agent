/target
/.idea/
/.vscode/
/.DS_Store
*.swp
*.swo
*.tmp
*.log

# Rust
**/*.rs.bk

# Build / coverage
/coverage/

# Local agent scratch / caches
/.hermes/
/.agent-hub/
/.chuang-audit/
/.chuang-file-audit/
/context/
/data/
# Public skill contract docs referenced by kernel_status readiness checks stay
# tracked even though the rest of data/ is local runtime state.
!/data/skills/external_agent_dispatch_sop.md
!/data/skills/unified_identity_engine_adapter.md
/tmp/
/$tmp/
/$work_dir/
/diagnostics/
/reports/
/artifacts/
/citywall_event_ui_mockups/
__pycache__/

# BFCL benchmark working tree (contains its own git repo, 200+ MB)
/.bfcl-ws/

# One-off task drafts at repo root (feishu/citywall intermediate)
/tmp-*/
/tmp-*.md
/tmp-*.json

# Generated media noise (tracked subscription-upgrade pngs stay force-added if needed)
/generated/*-image2.png

# Local backups / editor leftovers
/config.toml.bak*
**/*.rs.bak*
**/*.bak-*

# Release packaging / local archives (do not commit)
/chuang-agent-src-*.zip
/docs/larkcli/
/OPENSOURCE-PREP-CHECKLIST.md

# Internal ops docs (local-only, never publish)
/docs/*handoff*
/docs/*runbook*
/docs/*receipt*
/docs/*inventory*
/docs/*migration*
/docs/*upgrade*
/docs/dirty-tree*
/docs/local-*
/docs/pi-*
/docs/sub2-*
/docs/vultr-*
/docs/global-real-live*
/docs/live-*
/docs/receipts/

# Local external reference worktrees / downloaded helper repos
/ai-sdk-codex-oauth/
/gbrain-http-wrapper/
/gpt-token-tools/
/oc-codex-multi-auth/
/opencode-multi-auth-codex/

# Local operator config / generated identity runtime files
/config.toml
# evolver 外环运行时状态（观察流 + 规则变更 journal）不入库
/rules/.evolver/
# identity/ is 100% local-private runtime state (real SOUL/STORY/USER/MEMORY/agents.toml/diary)
# Never publish. Public identity templates live in config.example.toml / docs.
/identity/
/__IDENTITY__/

# OS / editor noise
Thumbs.db
/server-assets/
.chuang/

# Runtime diary (generated per-day by remember pipeline)
/identity/diary/

# local runtime env (real values, do not commit)
ops/systemd/*.env

# Local env files (real secrets stay untracked; keep *.env.example)
/ops/systemd/*.env
# Real systemd units contain local paths; only *.example are tracked
/ops/systemd/*.service
/ops/systemd/*.timer


# Internal MIMO vision-model logo description draft (not for publish)
/assets/mimo_desc.txt
