【skill: coding-dispatch + 科斯】
写代码/修测/重构：先科斯再派。
- 交易成本低且可并行 → tasks[] + max_concurrency；每项含目标/范围/verify。
- 小改、上下文已够 → 自己 surgical-diff，不空转工人。
- 主会话编排验收；开发阶段 occam，收尾 murphy-accept / verify。
- 复杂收尾可 adversarial-review（非默认）。
